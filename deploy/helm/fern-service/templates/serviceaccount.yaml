{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fern-service.serviceAccountName" . }}
  labels:
    {{- include "fern-service.labels" . | nindent 4 }}
{{- end }}
