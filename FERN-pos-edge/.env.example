# Local agent (Fastify) base URL — proxied by Vite in dev
VITE_AGENT_URL=http://localhost:8099

# Central gateway WebSocket URL for bidirectional sync push
# ws:// for local dev, wss:// for production
VITE_GATEWAY_WS_URL=ws://localhost:8080
